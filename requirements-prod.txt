# Production dependencies for DC Formatter
# Core document processing
python-docx==1.1.2
lxml==5.3.2

# Data processing (if actually used in pipeline)
pandas>=2.0.0
PyYAML==6.0.2
beautifulsoup4==4.13.3

# Utilities
requests==2.32.3
